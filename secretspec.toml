[project]
name = "nixos-pi-deployment"
revision = "1.0"
require_reason = "agents"

[profiles.default]
PI_WIREGUARD_PRIVATE_KEY = { description = "Pi WireGuard private key", as_path = true, type = "command", generate = { command = "wg genkey" } }

[profiles.pi]
