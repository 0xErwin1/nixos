# Glossary

Domain terms, internal nicknames, and user roles used in this project.
The testing pipeline uses this file to describe failures in plain language.

---

## Business and Product Terms

| Term | Definition |
|------|-----------|
| `<Term>` | `<What it means in this product's context>` |
| `<Term>` | `<What it means in this product's context>` |

---

## Internal Nicknames and Acronyms

Terms used internally that do not match the public-facing product language.

| Internal name | What it refers to | Public-facing name (if different) |
|---------------|------------------|----------------------------------|
| `<nickname>` | `<what it is>` | `<public name>` |
| `<acronym>` | `<what it stands for>` | — |

---

## User Roles and Permissions

| Role | Description | Can do | Cannot do |
|------|-------------|--------|-----------|
| `<Role>` | `<who this is>` | `<key permissions>` | `<key restrictions>` |
| `<Role>` | `<who this is>` | `<key permissions>` | `<key restrictions>` |

---

## Status Values

If the product has named statuses for entities (orders, tasks, tickets, etc.), list them here
so the testing pipeline can use the correct terms in test descriptions.

| Entity | Statuses | Notes |
|--------|---------|-------|
| `<Entity>` | `<status1, status2, ...>` | `<any transition rules worth knowing>` |
